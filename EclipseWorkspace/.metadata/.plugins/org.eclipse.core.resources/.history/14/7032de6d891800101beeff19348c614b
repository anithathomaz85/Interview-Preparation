package gts;

/**
 * how display all the headers . Write code
 * */
public class Q72_TheorySpringbootHeaders {
	
/*
 * 	@RestController
	@RequestMapping("/api")
	public class HeaderController {
	 
	   @GetMapping("/headers")
	   public ResponseEntity<String> showHeaders(@RequestHeader Map<String, String> headers) {
	       headers.forEach((key, value) -> {
	           System.out.println(key + " = " + value);
	       });
	 
	       return ResponseEntity.ok("Headers printed in console");
	   }
	}
 * 
 * */	

}
